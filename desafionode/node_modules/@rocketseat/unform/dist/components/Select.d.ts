import { SelectHTMLAttributes } from "react";
interface Option {
    id: string;
    title: string;
}
interface Props extends SelectHTMLAttributes<HTMLSelectElement> {
    name: string;
    label?: string;
    options: Option[];
}
export default function Select({ name, label, options, ...rest }: Props): JSX.Element;
export {};
